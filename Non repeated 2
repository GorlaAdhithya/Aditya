#include<stdio.h>
int main() {
    int a[100], i, j, n, count, elements;
    
    printf("Enter number of elements in array: ");
    scanf("%d", &n);
    
    printf("Enter the elements of the array: ");
    for(i = 0; i < n; i++) {
        scanf("%d", &a[i]);
    }
    count = 0;
    for(i = 0; i < n; i++) {
        int isUnique = 1;
        for(j = 0; j < i; j++) {
            if(a[i] == a[j]) {
                isUnique = 0;
                break;
            }
        }
        if(isUnique) {
            count++; // Increment count for unique elements
        }
      }
    printf("The number of unique elements is: %d\n", count);
    
    return 0;
}
