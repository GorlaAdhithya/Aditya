#include<stdio.h>
int main(){
   int n,*ptr;
   printf("enter a number of elements: ");
   scanf("%d",&n);
   printf("enter the elements of array :");
   int arr[n];
   for(int i=0;i<n;i++){
   	scanf("%d",&arr[i]);
   }
 ptr=arr;
 for(int i=0;i<n;i++){
   printf("%d",*(ptr+i));}
   return 0;
}
