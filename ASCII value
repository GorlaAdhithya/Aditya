#include <stdio.h> 
	int main (){
		int ascii_value;
		char ch;
		printf("enter the character :");
		scanf("%c",&ch);
 	  ascii_value =(int)ch ;
		printf("the ascii value of %c is %d",ch,ascii_value);
		return 0;
	}
